services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: predfit
      POSTGRES_PASSWORD: predfit
      POSTGRES_DB: predfit
    ports:
      - "5432:5432"
    volumes:
      - predfit_db:/var/lib/postgresql/data

volumes:
  predfit_db:

